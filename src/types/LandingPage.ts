export type TimelineCardProps = {
    date?: string;
    title?: string; 
    description?: string;
    imageUrl?: string;
    cta?: { text: string; link: string };
};

export type PreFooterCardProps = {
    imageurl: string
    text: string
    cta: { text: string; link: string };
};

